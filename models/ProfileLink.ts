class ProfileLink {
  title: string;
  url: string;
  imgUrl: string;

  constructor(title: string, url: string, imgUrl: string) {
    this.title = title;
    this.url = url;
    this.imgUrl = imgUrl;
  }
}

export default ProfileLink;
